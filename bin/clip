#!/usr/bin/env bash

set -eu -o pipefail

case "$XDG_SESSION_TYPE" in
    x11) xclip -selection clipboard "$@" ;;
    wayland) wl-copy "$@" ;;
    *)
        if [[ "$(uname)" == Darwin ]]; then
            pbcopy "$@"
        fi
        ;;
esac
